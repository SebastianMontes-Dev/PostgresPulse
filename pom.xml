<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.1</version>
        <relativePath/>
    </parent>

    <groupId>com.postgrespulse</groupId>
    <artifactId>postgrespulse</artifactId>
    <version>2.3.0</version>
    <name>PostgresPulse</name>
    <description>Plataforma enterprise de monitoreo, analisis y salud de bases de datos PostgreSQL</description>

    <properties>
        <java.version>21</java.version>
        <springdoc.version>3.1.0</springdoc.version>
        <resilience4j.version>2.4.0</resilience4j.version>
        <testcontainers.version>1.21.4</testcontainers.version>
        <jjwt.version>0.13.0</jjwt.version>
        <!-- postgresql.version ya no se fija a mano: el override de
             CVE-2026-54291 de la v2.1.0 quedó por debajo de lo que
             spring-boot-dependencies:4.1.1 gestiona por defecto (42.7.13). -->
        <!-- tomcat.version SÍ se fija: 11.0.24 (default de 4.1.1) sigue
             expuesto a CVE-2026-65182/65905/68525 (bypass de autenticacion/
             control de acceso), parcheados en la linea 11.0.x recien en
             11.0.25 — Trivy lo bloqueó en CI. Se mantiene en la linea 11.0.x
             (Jakarta EE 11/Servlet 6.1, requerido por Boot 4.1.1). -->
        <tomcat.version>11.0.25</tomcat.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <!-- Expone /actuator/prometheus (formato de texto Prometheus)
                 para las metricas de MetricasConfig y las de Micrometer. -->
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>

        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <!-- Spring Boot 4 saco la autoconfiguracion de Flyway a este
                 starter separado: sin el, flyway-core queda en el classpath
                 pero nunca corre (Hibernate valida el esquema contra una BD
                 vacia y falla en el arranque). -->
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-flyway</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>

        <dependency>
            <!-- resilience4j-spring-boot3 se autoverifica contra la version
                 de Spring Boot en el arranque y rechaza explicitamente Boot
                 4.x ("only compatible with Spring Boot 3.x"); el artefacto
                 correcto para esta version es resilience4j-spring-boot4. -->
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-spring-boot4</artifactId>
            <version>${resilience4j.version}</version>
        </dependency>

        <dependency>
            <!-- Autenticacion JWT: firma/validacion de tokens HS256. -->
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <!-- sec:authentication en las plantillas Thymeleaf del panel
                 (muestra el nombre del usuario autenticado en la barra de navegacion). -->
            <groupId>org.thymeleaf.extras</groupId>
            <artifactId>thymeleaf-extras-springsecurity6</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <!-- Spring Boot 4 saco @WebMvcTest/@AutoConfigureMockMvc/TestRestTemplate
                 de spring-boot-starter-test a este starter separado. -->
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <!-- Idem para @DataJpaTest/@AutoConfigureTestDatabase/TestEntityManager. -->
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <!-- spring-boot-resttestclient (TestRestTemplate) referencia
                 RestTemplateBuilder al introspeccionar su auto-configuracion,
                 aunque el modulo no forme parte de la app en si. -->
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-restclient</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>build-info</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <!--
                    Analisis estatico bloqueante: `mvnw verify` falla si hay
                    hallazgos Medium+ no excluidos. Empezo en modo reporte
                    (failOnError=false) para no romper CI con hallazgos
                    preexistentes; el reporte inicial (39 hallazgos) se
                    resolvio asi: los 18 EI_EXPOSE_REP/REP2 sobre records/DTOs
                    con campos List/Map se corrigieron con copia defensiva
                    (constructor compacto), los 2 sobre Map de entidades JPA
                    igual (detalleJson/detalle no son asociaciones, son JSON
                    simple), CT_CONSTRUCTOR_THROW en CifradoServicio se
                    corrigio haciendo la clase final (el constructor lanza si
                    la clave AES es invalida; sin "final" un subtipo podria
                    resucitar el objeto a medio construir via finalizer). Los
                    19 restantes son falsos positivos estructurales de
                    Spring/JPA que SpotBugs no puede distinguir de mutacion
                    externa real: documentados y excluidos uno a uno con
                    justificacion en spotbugs-exclude.xml (nunca una
                    exclusion de paquete completo).
                -->
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
                <version>4.10.4.0</version>
                <configuration>
                    <effort>Max</effort>
                    <threshold>Medium</threshold>
                    <failOnError>true</failOnError>
                    <excludeFilterFile>spotbugs-exclude.xml</excludeFilterFile>
                </configuration>
                <executions>
                    <execution>
                        <id>spotbugs-check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.15</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <!--
                            SPECS #12 exige "cobertura >=80% en nucleo de puntuacion", no en toda
                            la app: el panel de control y los filtros de seguridad son
                            composicion/presentacion, no el motor de analisis. El gate principal
                            cubre com.postgrespulse.analisis (chequeos, orquestador, puntuacion)
                            al 80%. servicio y programacion (exportacion, salud, tendencias,
                            ciclo programado) ahora tienen tests propios (ExportacionServicioTest,
                            ProgramadorAnalisisServicioTest) y se suman a un segundo umbral, mas
                            bajo porque incluyen mas codigo de mapeo DTO de bajo riesgo.
                        -->
                        <id>check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>PACKAGE</element>
                                    <includes>
                                        <include>com.postgrespulse.analisis</include>
                                        <include>com.postgrespulse.analisis.chequeos</include>
                                        <include>com.postgrespulse.analisis.soporte</include>
                                    </includes>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.80</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                                <rule>
                                    <element>PACKAGE</element>
                                    <includes>
                                        <include>com.postgrespulse.servicio</include>
                                        <include>com.postgrespulse.programacion</include>
                                    </includes>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.70</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                                <rule>
                                    <!--
                                        Tenian tests desde antes (JwtServicioTest, JwtAuthenticationFilterTest,
                                        SeguridadConfigIntegracionTest, RbacAutorizacionIntegracionTest, etc.)
                                        pero ningun gate exigia mantenerlos: justo el codigo mas sensible
                                        (JWT, filtros, endpoints REST) podia perder cobertura sin que CI lo
                                        notara. Umbral 80% fijado bajo la cobertura real medida al momento de
                                        agregar esta regla (seguridad ~94%, config 100%, controlador ~85%):
                                        deja margen sin inventar un numero que rompa el build de entrada.
                                    -->
                                    <element>PACKAGE</element>
                                    <includes>
                                        <include>com.postgrespulse.seguridad</include>
                                        <include>com.postgrespulse.config</include>
                                        <include>com.postgrespulse.controlador</include>
                                    </includes>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.80</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                                <rule>
                                    <!--
                                        com.postgrespulse.panel no tenia ningun gate: una regresion en
                                        PanelAuthControlador (login/logout del panel) no rompia el build.
                                        Ahora tiene test propio (PanelAuthControladorTest) y sube el
                                        paquete a ~73% medido. Umbral en 0.65, no en el 0.80 de
                                        seguridad/config/controlador, porque PanelControlador (el
                                        dashboard en si, ~23% de sus lineas) y CategoriaVistaDto siguen
                                        sin test propio; subir este umbral cuando los tengan.
                                    -->
                                    <element>PACKAGE</element>
                                    <includes>
                                        <include>com.postgrespulse.panel</include>
                                    </includes>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.65</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
